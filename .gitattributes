# =====================================================================
#  .gitattributes —— 换行符策略
# =====================================================================
#
#  为什么需要这个文件
#  ------------------
#  本机 core.autocrlf=true，git 会在提交时把文本文件的 CRLF 转成 LF。
#  但用户拿代码的方式是「GitHub 网页 → Code → Download ZIP」，而 GitHub
#  的 ZIP 直接打包**仓库里存的原始字节**（不会做 checkout 时的换行转换）。
#  结果：本地明明是 CRLF 的 .bat，用户下载解压后却变成裸 LF。
#
#  .bat 必须是 CRLF，两个原因：
#    1) cmd.exe 对裸 LF 的批处理文件在部分场景（标签 / goto 跳转、
#       跨行括号块）解析行为不稳定；
#    2) Windows 自带的记事本（旧版）打开裸 LF 文本会挤成一行。
#
#  所以这里用 -text 明确告诉 git：这些文件**不要做任何换行/编码转换**，
#  原样存取。代价是 git diff 会把它们当二进制显示（"Binary files differ"），
#  换来的是「用户下载到的就是开发者验证过的那份字节」。
#
#  改完 .bat / .txt 后请跑一下 `python check_bat.py` 确认仍是 CRLF。
# =====================================================================

# 批处理：纯 ASCII + CRLF，原样存取（详见上面说明）
*.bat  -text

# 给普通用户看的中文指引：必须 CRLF + UTF-8 BOM，记事本才能正确显示
SETUP-GUIDE.txt  -text
