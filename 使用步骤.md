# 使用步骤（照着做就行）

整个流程分 **准备**、**转换数据**、**启动插件**、**使用** 四步。  
其中「准备」只需要做一次，之后每次开机只需「转换 + 启动」。

---

## 一、准备工作（只做一次）

### 1. 开启达芬奇的外部脚本

1. 打开达芬奇（DaVinci Resolve）。
2. 顶部菜单栏点 **DaVinci Resolve** → **偏好设置（Preferences）**。
3. 左侧选 **系统（System）** → 右侧 **常规（General）**。
4. 找到 **External scripting using**，把它从 `None` 改成 **`Local`**。
5. 点 **保存（Save）**，然后**完全关闭并重新打开达芬奇**（这一步必须重启才生效）。

### 2. 安装置顶工具（可选，但强烈建议）

悬浮窗默认不会一直盖在达芬奇上面，需要装微软的 **PowerToys**：

- 打开微软商店（Microsoft Store），搜索 **PowerToys**，安装。
- 装好后，选中悬浮窗，按快捷键 **`Win + Ctrl + T`** 就能让它始终置顶。

---

## 二、转换课件数据（xlsx → JSON）

你的课件 Excel 会更新，所以**每次课件有改动后，都要重新转换一次**。

有两种方式：

**方式 A：双击（最简单）**

1. 进入插件目录（本项目解压/复制后所在的文件夹，即 `convert.bat`、`start.bat` 所在目录）。
2. 双击 **`convert.bat`**，自动读取桌面上的 **`冠军课程课件.xlsx`** 并转换。
3. 看到「转换完成」字样即成功。

**方式 B：拖拽（任意位置的文件）**

把任意位置的 xlsx 文件**拖到 `convert.bat` 图标上松开**，会转换你拖进来的那个文件。文件名不限。

> 想只转换某一节课：把 `convert.bat` 和你的课件 xlsx 放在同一目录，在文件资源管理器里把 xlsx **拖到** convert.bat 图标上即可。

---

## 三、启动插件

1. 确认**达芬奇已经打开**（并且已经做完第一步的外部脚本设置）。
2. 回到插件目录，双击 **`start.bat`**。
3. 会弹出两个窗口：
   - 一个黑色命令行窗口（标题「课程强度同步-后端」）—— **这是后端，别关**。
   - 一个**暗色悬浮窗** —— 这就是实时显示强度信息的小窗。
4. 选中悬浮窗，按 **`Win + Ctrl + T`** 让它置顶。

---

## 四、在达芬奇里使用

1. 在达芬奇里打开（或新建）一条时间线，**把时间线名字改成课程名**。
   - 例如时间线叫 `跑步机-进阶跑姿训练`（或简写 `进阶跑姿训练` 也能匹配）。
2. 拖动播放头（或按空格播放），悬浮窗就会**实时跟随**，显示当前时间点的：
   - 课程**环节**（大字）
   - **动作名称** + **强度关键词**（黄色标签）
   - **速度 / 配速 / 坡度**等指标
   - 下方一条**强度曲线**，带当前播放头位置竖线

这样剪辑师就能根据「当前这段是什么环节、什么强度」来自主决定切镜节奏了。

---

## 常见问题

| 现象                | 解决办法                                               |
| ----------------- | -------------------------------------------------- |
| 双击 `start.bat` 闪退 | 双击 `convert.bat` 看是否能正常转换；都不行说明 Python 没装好，告诉我报错内容 |
| 悬浮窗一直显示「正在连接达芬奇」  | 达芬奇没启动，或第一步的 External scripting 没设为 Local 并重启      |
| 悬浮窗显示「未找到同名课程数据」  | 时间线名字和课程名对不上；改成课程名（如 `跑步机-进阶跑姿训练`）                 |
| 悬浮窗显示「无法连接服务」     | 后端窗口被关了，重新双击 `start.bat`                           |
| 悬浮窗盖不住达芬奇         | 没置顶，选中悬浮窗按 `Win + Ctrl + T`                        |

---

## 两个脚本的作用

- **`convert.bat`**：把课件 Excel 转成 JSON 数据（课件更新后要重新跑）。
- **`start.bat`**：启动「后端 + 悬浮窗」，用于剪辑时实时看强度。
