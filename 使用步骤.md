# 使用步骤（照着做就行）

整个流程分 **准备**、**转换数据**、**启动插件**、**使用** 四步。  
其中「准备」只需要做一次，之后每次开机只需「转换 + 启动」。

> **换了一台新电脑？** 先看下面的「第零步」，跑一次环境检查再回来按顺序做。

---

## 零、换了新电脑 / 第一次在这台电脑上用

**先双击 `检查环境.bat`。**

它会逐项检查这台电脑能不能跑插件，并打印一份中文报告：

- Python 装没装、版本对不对（必须是 **3.10 或 3.11**）
- 达芬奇装没装、**装在哪**（`fusionscript.dll` 有没有找到）、什么版本、外部脚本开没开
- **真连一次达芬奇**：报出当前工程、当前时间线、能不能匹配到课件（这是最关键的一项）
- Edge 在不在（悬浮窗要用它）
- 课件转没转、端口有没有被占（如果插件正开着，还会顺带打出「那个后端现在看到了什么」）

每项后面都写着「怎么办」。全部显示 `[通过]` 就可以往下走了。

> 达芬奇装在哪不用你操心：**插件自己会找**（先看正在运行的达芬奇，再看
> Windows 安装记录、快捷方式、各磁盘常见目录，最后还会做一次限定深度的全盘
> 搜索），找到一次就记住。只有极个别情况才需要在 `config.json` 里手动填
> `resolve_install_dir`，报告里会告诉你。

### 关于手动改 `config.json`（重要，改错了插件会「假装没看见」）

新版已经能**容忍**下面这些常见写法，会自动修好并继续用，不用你重存：

- 记事本「另存为 UTF-8」产生的 **BOM**、或存成了 **ANSI**（中文不会变乱码）；
- 多写一个**尾逗号**、写了 `// 注释`；
- 路径写成**单反斜杠**（`"D:\软件\达芬奇"`）——旧版会整份配置被丢弃，**这正是
  「明明填了达芬奇目录却还是显示等待达芬奇…」的原因之一**。

不过还是建议按标准写法填，省得出现别的毛病：

```json
{
  "resolve_install_dir": "D:/软件/达芬奇"
}
```

**路径用正斜杠 `/` 最省事**（不用管转义）；一定要用反斜杠就写**两条** `D:\\软件\\达芬奇`。
改完直接重新双击 `start.bat` 即可。

> 万一还是写坏了也不用担心：后端**不会崩**，它会把默认设置跑起来，并把
> 「config.json 有语法错误（第 N 行）」这句话直接显示在悬浮窗底部状态栏，
> 同时写进体检报告 —— 你不会再遇到「改了却毫无反应、也不知道为什么」。

**如果它说找不到 Python**：双击 `start.bat` 时会自动弹出一份
`SETUP-GUIDE.txt` 中文安装指引，照着装一个 Python 3.11 就行
（安装时**务必勾选** `Add python.exe to PATH`，这是最容易漏的一步）。
如果这台电脑没有管理员权限、装不了 Python，指引里还有「便携版 Python」的办法：
从一台已经能用的电脑上把 Python 文件夹拷到本项目的 `runtime\python\` 里，不用安装。

---

## 一、准备工作（只做一次）

### 1. 开启达芬奇的外部脚本

1. 打开达芬奇（DaVinci Resolve）。
2. 顶部菜单栏点 **DaVinci Resolve** → **偏好设置（Preferences）**。
3. 左侧选 **系统（System）** → 右侧 **常规（General）**。
4. 找到 **External scripting using**，把它从 `None` 改成 **`Local`**。
5. 点 **保存（Save）**，然后**完全关闭并重新打开达芬奇**（这一步必须重启才生效）。

### 2. 不用装任何置顶工具了

悬浮窗**自带「始终置顶」**（和 PowerToys 的 Always On Top 效果一样），开出来就压在达芬奇上面，
不用再按 `Win + Ctrl + T`，也不用装 PowerToys。

**想取消置顶**：点悬浮窗**左上角那个图钉按钮**就行。

- 图钉**亮着（蓝色）** = 一直压在达芬奇上面；
- 图钉**灭了（灰色）** = 普通窗口，可以被别的窗口盖住，也能**正常最小化**。

你的选择会被记住：这次取消了置顶，下次打开插件也不会再自动置顶。
想让它从一开始就置顶，把 `config.json` 里的 `"always_on_top"` 改成 `true`（默认就是 `true`）。

---

## 二、转换课件数据（xlsx → JSON）

你的课件 Excel 会更新，所以**每次课件有改动后，都要重新转换一次**。两种方式，选一种就行：

**方式 A：双击 `convert.bat`**（不用开插件也能用）

1. 屏幕中间会弹出一个**文件选择窗口**（默认打开你上次选过的目录，第一次是「桌面」）。
2. 选中你的课件表格（比如 `冠军课程课件.xlsx`），点**打开**。
   - **可以一次选多个**：按住 `Ctrl` 一个个点，或按住 `Shift` 框选一片，
     然后点「打开」，它们会在一个窗口里挨个转完（现在课件常是「一节课一个表」，这个很省事）。
3. 黑窗口里滚动一堆「xxx -> xxx.json」就是转换成功。
4. 看到「全部完成：N 个文件，共成功转换 M 个课件」后，按任意键关闭窗口。

> - 选错了想重来：随时点「取消」，不会改动任何东西。
> - 也可以把任意位置的 xlsx **直接拖到 `convert.bat` 图标上**（一次拖多个也行），
>   会跳过选择框直接转换这些文件。
> - 就算有几个文件转换失败，成功的那些也照样转好了，最后会一起告诉你哪几个没转成。

**方式 B：在悬浮窗右下角点按钮**（插件开着的时候最方便，不用退出插件）

- 有课件数据时，按钮是 **「清除课件缓存」**；点它会把 `data/` 里已转换的 JSON 全部清空，
  按钮随即变成 **「选择课件」**。
- 点 **「选择课件」** 就会弹出和方式 A 一模一样的文件选择窗口，选好课件后自动转换。
- 转换成功后按钮自动变回 **「清除课件缓存」**，中间不用退出插件、也不用重启。

> 注意：转换时**不要**再点第二次按钮，等按钮从「等待选择…」变回来即可。

---

## 三、启动插件

1. 回到插件目录，双击 **`start.bat`**。
   - **达芬奇可以开着，也可以还没开** —— 插件会自己在后台等待，达芬奇启动后自动连上，
     不需要重启插件（见下方说明）。
2. 启动窗口会**自己关掉**，稍等 2~3 秒，屏幕上会弹出**暗色悬浮窗**。
   - 悬浮窗初始尺寸约是屏幕的 1/7 宽、1/5 高（约 260×230 的观感），不会挡住剪辑界面。
     想改的话，见 README 里的 `overlay_window_ratio` / `overlay_window_size` 配置。
   - 后端在后台静默运行，**不会再有黑色命令行窗口挂着**。
   - 万一没弹出悬浮窗，会有一个提示框告诉你原因（日志在 `.runtime/server.log`）。
3. **悬浮窗会自己置顶**，不用再做任何操作；不想置顶就点左上角的图钉取消。

**用完之后，直接关掉悬浮窗就行** —— 后端的后台进程会跟着自动退出，不会残留、也不会占着端口。

> **达芬奇顺序随意**：插件每 3 秒会重试一次连接。所以「先开插件、再开达芬奇」也完全没问题，
> 达芬奇一启动、打开时间线，悬浮窗就会自动开始同步。反过来达芬奇中途关掉再打开也一样。

---

## 四、在达芬奇里使用

1. 在达芬奇里打开（或新建）一条时间线，**把时间线名字改成课程名**。
   - 例如时间线叫 `跑步机-进阶跑姿训练`（或简写 `进阶跑姿训练` 也能匹配）。
   - **建议带上器械前缀**（跑步机 / 单车 / 划船机 / 椭圆机 / 徒手）。因为不同器械可能有同名课程，插件靠「器械 + 课程名」区分，带上前缀才不会张冠李戴。
2. 拖动播放头（或按空格播放），悬浮窗就会**实时跟随**，显示当前时间点的：
   - 课程**环节**（大字）
   - **动作名称** + **强度关键词**（黄色标签）
   - **速度 / 配速 / 坡度**等指标
   - 下方一条**强度曲线**，带当前播放头位置竖线

这样剪辑师就能根据「当前这段是什么环节、什么强度」来自主决定切镜节奏了。

---

## 常见问题

| 现象 | 解决办法 |
| --- | --- |
| **双击 bat 时滚出一堆「××× 不是内部或外部命令」**（多半发生在别的电脑上） | 旧版本的问题：bat 文件的编码和系统代码页不匹配。新版已把所有 bat 改成纯 ASCII，请重新到 GitHub 下载最新版 |
| 双击 `convert.bat` 没弹选择框 | 说明 Python 没装好。**双击 `检查环境.bat`** 看报告，按提示补装 Python 3.11 |
| 双击 `start.bat` 没反应 | 正常情况下本来就「没反应」——插件是后台静默运行的，一两秒后悬浮窗会自己弹出来。如果一直没窗口，最新版会弹一个中文提示框说明原因；连提示框都没有就双击 `检查环境.bat` 截图反馈 |
| 弹窗提示「找不到 Python」 | 那台电脑没装 Python。跟着自动打开的 `SETUP-GUIDE.txt` 装一个 Python 3.11（**务必勾选 Add python.exe to PATH**）；没有管理员权限就用里面的「便携版 Python」方案 |
| 弹窗提示「Python 版本 3.12+」 | 达芬奇只认 3.10 / 3.11，另装一个 3.11 和现有版本共存即可 |
| 双击 `start.bat` 弹「启动失败」 | 按提示框里的日志内容排查，多半是 Python 环境问题。日志在 `.runtime/server.log` |
| 这台电脑到底能不能跑？ | 双击 `检查环境.bat`，会打印中文体检报告并存到 `.runtime\环境体检报告.txt` |
| 悬浮窗一直显示「等待达芬奇…」 | 达芬奇还没启动，或第一步的 External scripting 没设为 Local 并重启。**启动达芬奇后插件会自动连上，不用重启插件** |
| 达芬奇明明开着、外部脚本也设了，还是连不上 | 多半是**没找到 `fusionscript.dll`**——这个文件在达芬奇安装目录里，而每个人的安装位置都不一样（有人 `C:\Program Files\...`，有人 `D:\软件\达芬奇`，有人 `E:\Davinci`）。双击 `检查环境.bat` 看【2】和【3】：报告会列出插件找过哪些目录、以及你时间线名字匹配没匹配上 |
| 体检报告里写「未找到 fusionscript.dll」 | 先把达芬奇打开再跑一次体检（进程在跑时最好找）。还不行就手动指定：开始菜单右键「DaVinci Resolve」→ 更多 → 打开文件位置，把那个目录填进 `config.json` 的 `"resolve_install_dir"`（路径用正斜杠最省事，如 `D:/软件/达芬奇`） |
| **明明在 `config.json` 填了达芬奇目录，还是显示「等待达芬奇…」** | 先查两件事：① 悬浮窗**底部状态栏**有没有红字说 config.json 有问题（有就按它说的改，多半是路径写法或编码）；② 双击 `检查环境.bat`，看【2】里插件找到两个关键文件没、【3】连接实测的结果。旧版遇到 config.json 格式错误会**整份丢弃**（后端直接起不来），新版已修 |
| 悬浮窗底部出现「config.json 有语法错误（第 N 行）」 | 你的 config.json 被写坏了。最省事：路径全用正斜杠，如 `"resolve_install_dir": "D:/软件/达芬奇"`；或把这一项改回 `null` 让插件自动找 |
| 插件解压在 **D 盘**（或别的盘）会有问题吗 | **不会，和解压在哪个盘完全无关** —— 插件所有路径都按自身文件夹解析。显示「等待达芬奇…」只可能是达芬奇那边的事：外部脚本没设成 Local 并重启 / 免费版不支持 / 没找到 `fusionscript.dll` / 时间线名对不上 |
| 体检报告里写「磁盘上发现有 2 份达芬奇安装」 | 以前装过别的版本、卸载没删干净。插件会按注册表版本自动挑对的那份（报告里标 `(*)`），不影响使用；想彻底干净就把旧目录删掉 |
| 悬浮窗显示「找不到指定的模块 / DLL load failed」 | 也是路径问题（找到的 dll 不是达芬奇正在用的那份）。按上面两条处理 |
| 拖动时间线时，画面内容「不动」一两秒 | 正常现象：拖拽时达芬奇不会及时把播放头位置吐出来，插件会**保留上一帧的画面**（不会闪白），你松手停下后立刻刷新 |
| 悬浮窗显示「已连接达芬奇，但当前没有打开的时间线」 | 达芬奇里还没打开任何时间线，打开一条就会自动同步 |
| 悬浮窗显示「未找到对应课程的强度数据」 | 时间线名字和课程名对不上。确认时间线名带器械前缀（如 `椭圆机-燃脂间歇训练`），且已跑过 `convert.bat` |
| 悬浮窗显示「无法连接服务」 | 后端没了，重新双击 `start.bat` |
| 想临时不置顶 | 点悬浮窗**左上角的图钉按钮**取消（会被记住，下次打开也不再置顶） |
| 悬浮窗太大 / 太小 | 改 `config.json` 里的 `overlay_window_ratio`（按屏幕比例）或 `overlay_window_size`（固定像素），重启插件生效 |
| 想清空所有课程数据重来 | 点悬浮窗右下角「清除课件缓存」，按钮会变成「选择课件」，直接点它重新导入课件即可 |
| 爬楼机课程不显示「阻力」 | 正常：早期爬楼机课件里阻力列是 0/空，没值就隐藏；有阻力值的课件（如磁控机型）会自动显示 |
| 一次转了 6 个文件，`data/` 里却没多 6 个 | 说明其中几个课件的工作表名重名了，转出来的 JSON 互相覆盖。看黑窗口里列的「工作表 -> JSON 文件名」对一下 |
| 右下角按钮显示「等待选择…」 | 说明文件选择窗口已经弹出来了（可能被挡在后面），在任务栏/屏幕中间找一下那个窗口 |

---

## 脚本的作用

- **`检查环境.bat`**：体检这台电脑能不能跑（Python / 达芬奇 / Edge / 课件 / 端口），
  给出中文报告和补救办法。**换新电脑第一件事就是双击它。**
- **`convert.bat`**：弹窗选课件（可多选、可拖多个文件上去）→ 转成 JSON 数据（课件更新后要重新跑）。
- **`start.bat`**：一键启动「后台后端 + 悬浮窗」，关掉悬浮窗就全部退出。
- **`launcher.py`**：`start.bat` 背后干活的脚本（启动前自检 → 起后端 → 开窗 → 跟随图钉保持置顶 → 收尾），
  平时不用管它。它出错会弹中文提示框，不会悄无声息。
- **`env_check.py`**：环境体检的实现，`检查环境.bat` 调用的就是它。
- **`probe_resolve.py`**：体检里「达芬奇连接实测」那一步的探针——真连一次达芬奇，
  报出当前工程/时间线/能否匹配到课件。连着插件一起交付，不用单独跑。
- **`SETUP-GUIDE.txt`**：中文安装指引。找不到 Python 时 `start.bat` 会自动用记事本打开它。
- **悬浮窗右下角按钮**：有数据时是「清除课件缓存」，清空后自动变成「选择课件」——
  等于把 `convert.bat` 搬进了悬浮窗，不用退出插件就能重新导入课件。
- **`check_bat.py`**（开发者用）：检查所有 `.bat` 是不是纯 ASCII + CRLF，
  防止以后又混进 GBK 编码导致换台电脑就乱码。改完 bat 跑一下 `python check_bat.py`。

### 换台电脑怎么装

1. 把整个项目文件夹复制（或解压）过去。
2. 双击 `检查环境.bat`，按报告补缺的东西（一般是装 Python 3.11）。
3. 装不了 Python 的机器：从能用的电脑上拷一个 Python 文件夹到 `runtime\python\`，
   插件会优先用它。整个文件夹（含 `runtime`）打包给同事，对方解压即用。
