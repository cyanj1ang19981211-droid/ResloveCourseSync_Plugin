# 使用步骤（照着做就行）

整个流程分 **准备**、**转换数据**、**启动插件**、**使用** 四步。  
其中「准备」只需要做一次，之后每次开机只需「转换 + 启动」。

---

## 一、准备工作（只做一次）

### 1. 开启达芬奇的外部脚本

1. 打开达芬奇（DaVinci Resolve）。
2. 顶部菜单栏点 **DaVinci Resolve** → **偏好设置（Preferences）**。
3. 左侧选 **系统（System）** → 右侧 **常规（General）**。
4. 找到 **External scripting using**，把它从 `None` 改成 **`Local`**。
5. 点 **保存（Save）**，然后**完全关闭并重新打开达芬奇**（这一步必须重启才生效）。

### 2. 安装置顶工具（可选，但强烈建议）

悬浮窗默认不会一直盖在达芬奇上面，需要装微软的 **PowerToys**：

- 打开微软商店（Microsoft Store），搜索 **PowerToys**，安装。
- 装好后，选中悬浮窗，按快捷键 **`Win + Ctrl + T`** 就能让它始终置顶。

---

## 二、转换课件数据（xlsx → JSON）

你的课件 Excel 会更新，所以**每次课件有改动后，都要重新转换一次**。

**双击 `convert.bat`** 就行：

1. 屏幕中间会弹出一个**文件选择窗口**（默认停在「桌面」）。
2. 选中你的课件表格（比如 `冠军课程课件.xlsx`），点**打开**。
3. 黑窗口里滚动一堆「xxx -> xxx.json」就是转换成功。
4. 看到「转换完成：成功 N 个课件」后，按任意键关闭窗口。

> - 选错了想重来：随时点「取消」，不会改动任何东西。
> - 也可以把任意位置的 xlsx **直接拖到 `convert.bat` 图标上**，会跳过选择框直接转换那个文件。

---

## 三、启动插件

1. 回到插件目录，双击 **`start.bat`**。
   - **达芬奇可以开着，也可以还没开** —— 插件会自己在后台等待，达芬奇启动后自动连上，
     不需要重启插件（见下方说明）。
2. 启动窗口会**自己关掉**，稍等 2~3 秒，屏幕上会弹出**暗色悬浮窗**。
   - 悬浮窗初始尺寸约是屏幕的 1/7 宽、1/5 高（约 260×230 的观感），不会挡住剪辑界面。
     想改的话，见 README 里的 `overlay_window_ratio` / `overlay_window_size` 配置。
   - 后端在后台静默运行，**不会再有黑色命令行窗口挂着**。
   - 万一没弹出悬浮窗，会有一个提示框告诉你原因（日志在 `.runtime/server.log`）。
3. 选中悬浮窗，按 **`Win + Ctrl + T`** 让它置顶。

**用完之后，直接关掉悬浮窗就行** —— 后端的后台进程会跟着自动退出，不会残留、也不会占着端口。

> **达芬奇顺序随意**：插件每 3 秒会重试一次连接。所以「先开插件、再开达芬奇」也完全没问题，
> 达芬奇一启动、打开时间线，悬浮窗就会自动开始同步。反过来达芬奇中途关掉再打开也一样。

---

## 四、在达芬奇里使用

1. 在达芬奇里打开（或新建）一条时间线，**把时间线名字改成课程名**。
   - 例如时间线叫 `跑步机-进阶跑姿训练`（或简写 `进阶跑姿训练` 也能匹配）。
   - **建议带上器械前缀**（跑步机 / 单车 / 划船机 / 椭圆机 / 徒手）。因为不同器械可能有同名课程，插件靠「器械 + 课程名」区分，带上前缀才不会张冠李戴。
2. 拖动播放头（或按空格播放），悬浮窗就会**实时跟随**，显示当前时间点的：
   - 课程**环节**（大字）
   - **动作名称** + **强度关键词**（黄色标签）
   - **速度 / 配速 / 坡度**等指标
   - 下方一条**强度曲线**，带当前播放头位置竖线

这样剪辑师就能根据「当前这段是什么环节、什么强度」来自主决定切镜节奏了。

---

## 常见问题

| 现象 | 解决办法 |
| --- | --- |
| 双击 `convert.bat` 没弹选择框 | 说明 Python 没装好。双击后看黑窗口里的报错，反馈给我 |
| 双击 `start.bat` 没反应 / 弹「启动失败」 | 按提示框里的日志内容排查，多半是 Python 环境问题。日志在 `.runtime/server.log` |
| 悬浮窗一直显示「等待达芬奇…」 | 达芬奇还没启动，或第一步的 External scripting 没设为 Local 并重启。**启动达芬奇后插件会自动连上，不用重启插件** |
| 悬浮窗显示「已连接达芬奇，但当前没有打开的时间线」 | 达芬奇里还没打开任何时间线，打开一条就会自动同步 |
| 悬浮窗显示「未找到对应课程的强度数据」 | 时间线名字和课程名对不上。确认时间线名带器械前缀（如 `椭圆机-燃脂间歇训练`），且已跑过 `convert.bat` |
| 悬浮窗显示「无法连接服务」 | 后端没了，重新双击 `start.bat` |
| 悬浮窗盖不住达芬奇 | 没置顶，选中悬浮窗按 `Win + Ctrl + T` |
| 悬浮窗太大 / 太小 | 改 `config.json` 里的 `overlay_window_ratio`（按屏幕比例）或 `overlay_window_size`（固定像素），重启插件生效 |
| 想清空所有课程数据重来 | 点悬浮窗右下角「清除课件缓存」，然后重新跑 `convert.bat` |

---

## 脚本的作用

- **`convert.bat`**：弹窗选课件 → 转成 JSON 数据（课件更新后要重新跑）。
- **`start.bat`**：一键启动「后台后端 + 悬浮窗」，关掉悬浮窗就全部退出。
- **`launcher.py`**：`start.bat` 背后干活的脚本（起后端、开窗、收尾），平时不用管它。
