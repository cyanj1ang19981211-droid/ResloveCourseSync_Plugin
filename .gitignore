# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
build/
dist/

# 虚拟环境
venv/
.venv/
env/

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo
*~

# 系统文件
Thumbs.db
.DS_Store
desktop.ini

# 本地配置（含本机达芬奇脚本路径等，不应提交）
config.local.json

# 日志 / 缓存
*.log
*.tmp
*.cache

# 运行时目录（后端日志等，start.bat 启动时自动生成）
.runtime/

# 便携版 Python（体积很大，各机器自行准备，不入库）
# 没有管理员权限装 Python 的机器，可以把 Python 拷到 runtime\python\ 里
runtime/

# 手动调试脚本（临时用，非正式功能）
# 注意：probe_resolve.py 不在这里 —— 它是「检查环境.bat」正式依赖的
# 连接实测探针，必须随包提交，不能忽略。
probe_raw.py

# 课程数据（核心业务数据，禁止提交/上传）
data/
*.xlsx
