# -*- coding: utf-8 -*-
"""版本号（唯一的一份）。

为什么值得单独一个文件：
    用户报「后端启动失败」时，第一件要确认的事就是**他手上的到底是哪一版**。
    以前没有任何版本标识 —— 日志、弹窗、体检报告里都看不出新旧，于是同一个
    早就修好的问题（例如 config.json 写坏导致后端崩）会被反复当成新 bug 报过来。

    所以：启动日志第一行、启动失败弹窗、体检报告、/diag 接口都会带上这个版本号。
    让同事报问题时先报版本号，就能立刻判断他有没有重新下载 ZIP。
"""

# 改动契约：任何影响用户可见行为的改动都要 +1（主版本.次版本.修订号）。
VERSION = "0.7.0"

# 这次发布改了什么（出现在体检报告与启动日志里，方便口头核对）
HIGHLIGHTS = (
    "悬浮窗用自己的浏览器独立档案，不和用户自己的 Edge 打架",
    "退出时按进程树清理，不再留下占用端口的后端",
    "端口被占自动换端口，不再弹「后端启动失败」",
    "时间线起点不是 00:00:00:00 时自动对齐（不勾「使用项目设置」的时间线也能同步）",
)


def describe() -> str:
    """一行版本描述，用于日志/弹窗。"""
    return "课程强度同步 v" + VERSION
